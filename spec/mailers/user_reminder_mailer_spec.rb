require 'rails_helper'

RSpec.describe UserReminderMailer, type: :mailer do
  # describe 'instructions' do
  #   let(:user) { create(:user) }
  #   let(:booking) { create(:booking) }
  #   let(:mail) { described_class.remind_mail(booking, user).deliver_later(wait_until: booking.start_at - 1.hour) }
  #
  #   it 'renders the subject' do
  #     expect(mail.subject).to eq('You booked a time for the service')
  #   end
  #
  #   it 'renders the receiver email' do
  #     expect(mail.to).to eq(user.email)
  #   end
  # end
end

